model Supplement {
  id         String   @id @default(uuid())
  name       String
  shortDesc  String
  fullDesc   String
  mechanisms String[]
  evidence   Evidence
  favoredBy  User[]
}

enum Evidence {
  HIGH
  MODERATE
  LOW
  INSUFFICIENT
}
